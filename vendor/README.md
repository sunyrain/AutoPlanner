# Vendor Checkouts

This directory is for external baselines that must remain outside the
AutoPlanner package. Third-party source checkouts are ignored by git.

Current external baseline:

- `vendor/ChemEnzyRetroPlanner/`: upstream ChemEnzyRetroPlanner clone used only
  through the thin AutoPlanner adapter and smoke scripts.

